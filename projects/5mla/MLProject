name: MLflow Project
conda_env: conda.yaml

entry_points:
  main:
    parameters:
      train_path: {type: str, description: "Path to the training dataset"}
      model_param1: {type: float, default: 0.1, description: "Some parameter of the model"}
    command: "python train.py -r {train_path} {model_param1}"

